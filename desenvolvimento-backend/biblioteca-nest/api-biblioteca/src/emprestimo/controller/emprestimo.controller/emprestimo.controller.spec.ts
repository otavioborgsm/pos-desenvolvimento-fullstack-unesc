import { EmprestimoController } from './emprestimo.controller';

describe('EmprestimoController', () => {
  it('should be defined', () => {
    expect(new EmprestimoController()).toBeDefined();
  });
});
